To use this app you will need to have postgres and node on you local machine. 

Go ahead and create a database, with a user and a passoword in postgres. Make sure that postgres is up and running. Go to App/example.env and fill out your creadentials. Rename the file to just ".env". 

Cd into the App folder and run npm start. Your server will now be running on your specified port. 